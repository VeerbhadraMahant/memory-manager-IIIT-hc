# Minimum pins rather than exact ones: a hackathon does not need byte-reproducible
# installs, and an unresolvable exact pin costs an hour at the worst time.
fastapi>=0.115
uvicorn[standard]>=0.32
psycopg[binary,pool]>=3.2
pgvector>=0.3.6
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0
google-genai>=1.0
httpx>=0.27
